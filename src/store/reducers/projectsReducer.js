

const initialState = {
    projects : null,
    completedProjects : null
}

export default (state=initialState,action) => {
    switch(action.type){
        default : 
            return state
    }
}